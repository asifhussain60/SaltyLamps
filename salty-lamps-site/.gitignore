# Dependencies
node_modules/

# Build output
dist/

# Environment — contains API keys, never commit
.env
.env.local
.env.*.local
.dev.vars

# Vite temp files
vite.config.js.timestamp-*

# Wrangler local dev state (local D1 sqlite, cache — never real data)
.wrangler/

# macOS
.DS_Store

# Aura video generator: local render cache + pre-overwrite backups
.aura-render-cache/
*.bak
